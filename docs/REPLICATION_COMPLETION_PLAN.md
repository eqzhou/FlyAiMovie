# FlyAiMovie 全量复刻完成计划

> 生成日期：2026-07-25
> 完成日期：2026-07-25
> 依据：`docs/FUNCTIONAL_PARITY.md` + 当前代码现状
> 目标：完成用户可观察功能对等（L1），并推进可自动完成的发布门槛（L2）
> 状态：**L1 已完成**；**可自动 L2 已完成**；外部账号/法务 L2 仍开放

## 1. 完成定义

### L1 功能对等收口（产品可用、入口一致） — ✅ 已完成
所有用户可观察管理能力都有完整入口，不再出现「后端能做、某页只能看」。

### L2 发布级完成（可受控上线） — 🟡 部分完成
- 本机可自动项：SBOM、依赖漏洞、Compose 冷启动/备份 — ✅
- 真实账号 smoke + 正式 SPDX 法务归档 — ⬜ 依赖外部

说明：
- L1 做完 = 复刻完成
- L2 是商用发布门槛

## 2. 差异分层

### A. 已复刻主能力
公开能力 `P-01 ~ P-28`、非功能 `N-01 ~ N-16` 主流程大多已验收，覆盖：

- 项目/剧集/角色/场景/道具/分镜 CRUD 与归属
- 五类 Agent、提示词模板、宫格、帧/视频/TTS、合成导出
- 自动制作状态机、任务中心、素材库、角色模板库
- 组织、邀请、配额、审计、缓存、安全边界
- Mock 全链路与自动化测试

### B. 产品入口缺口（计划重点） — ✅ 已清空

| 区域 | 现状 | 缺口 |
|------|------|------|
| DramaView 角色 | 完整 CRUD + 生成图/试听/存库 | 已齐 |
| DramaView 场景 | 完整 CRUD + 生成图/复制迁移 | 已齐 |
| DramaView 道具 | 已有增删改+生成图 | 已齐 |
| Workbench 角色/场景 | 增删改、生成、角色库、场景迁移 | 已齐 |
| Home 项目 | 已可编辑元数据 | 已齐 |
| 剧集生命周期 | 增删/复制/上下移/编辑标题与 AI 配置 | 已齐 |

### C. 有意保留的 clean-room 差异

- 后端 Go + GORM，不是上游 TS/Mastra/Drizzle
- 独立 API/模型/提示词/UI 信息架构
- 不做像素级 UI 复刻，不做品牌/文案/素材复用

### D. 商业发布门槛（L2）

| 项 | 状态 |
|----|------|
| 真实厂商 smoke：OpenAI / MiniMax / 火山 / Vidu / 阿里 | ⬜ 需 `SMOKE_*` 密钥 |
| 真实 SMTP 邀请 + 密码恢复 | ⬜ 需 `SMOKE_SMTP_*` |
| 干净 Docker/PostgreSQL 冷启动、卷恢复、备份回滚 | ✅ 本机 Colima 已演练 |
| SBOM、依赖漏洞扫描 | ✅ `make sbom` + govulncheck 0 + npm audit 0 |
| 正式 SPDX 许可证结论 / 法务归档 | ⬜ 发布流程人工完成 |
| FFmpeg 构建合规记录 | ✅ 已进 `artifacts/sbom/` |
| GitHub Actions `verify` 归档 | ⬜ 账单/额度恢复后重跑 |

## 3. 执行阶段

### Phase 0 — 冻结边界 — ✅
- 以 `docs/FUNCTIONAL_PARITY.md` 为唯一验收源
- 只补「用户可观察缺口」，不主动发明新功能
- 每完成一块：测通 → 提交 → 推主干 → 回矩阵勾状态 → 再下一块

### Phase 1 — 资源管理入口对齐 — ✅
1. DramaView 角色管理：新增/编辑/删除、生成形象、音色绑定/试听、存入角色库
2. DramaView 场景管理：新增/编辑/删除、生成场景图、复制/迁移（`sceneAPI.copy/move`）
3. 一致性：viewer 只读；失败提示与道具编辑同一交互语言
4. 测试：`frontend/tests/e2e/account-settings-assets.spec.ts` 覆盖资产页角色/场景/剧集编辑

**验收：** 不进工作台也能完整管理项目角色/场景。 — ✅

### Phase 2 — 剧集与项目元数据打磨 — ✅
1. 剧集：项目页「编辑标题/绑定 AI 配置」
2. 项目：Home 编辑字段完整（标题/描述/默认配置）
3. 删除级联提示文案已提示软删会带走依赖

**验收：** 项目与剧集生命周期在 UI 上闭环。 — ✅

### Phase 3 — 工作台体验对等补齐（可选） — 降级不阻塞
项目页道具/角色/场景已齐；工作台主流程 E2E 已覆盖。可选体验项不阻塞 L1。

### Phase 4 — 矩阵与文档回写 — ✅
1. `FUNCTIONAL_PARITY.md`：入口已齐写进 P-04/P-05 等条目
2. `CLEAN_ROOM.md` 简表已勾齐
3. 明确仅剩发布门槛，避免再次被理解成「功能没做完」

### Phase 5 — 发布门槛（L2） — 🟡
1. 真实厂商 smoke — ⬜ 外部
2. 真实 SMTP smoke — ⬜ 外部
3. Docker 双模式冷启动 + 恢复演练 — ✅
4. `make sbom` + 漏洞扫描 — ✅
5. 发布证据包路径已就绪（commit、SBOM、ffmpeg license）；CI 归档待账单恢复

## 4. 建议执行顺序（历史）

| 优先级 | 事项 | 状态 |
|--------|------|------|
| P0 | DramaView 角色 CRUD + 生成/试听 | ✅ `62331fd` |
| P0 | DramaView 场景 CRUD + 生成 + copy/move | ✅ `62331fd` |
| P1 | 剧集编辑入口补齐 | ✅ 同批 + E2E |
| P1 | 文档矩阵回写 | ✅ `5d9f302` 等 |
| P2 | 工作台道具/体验小补（可选） | 不阻塞 |
| P3 | 真实厂商/SMTP/正式 SPDX | ⬜ 外部 |

## 5. 不做清单

- 不追上游每一个开发版 UI 细节
- 不把「厂商字段 100% 对齐」当阻塞项
- 不在没有真实账号时把 live smoke 标成已完成
- 不在 L1 完成后继续无限「盘点下一高价值缺口」

## 6. 完成结论

**复刻（L1）已完成。** 用户可观察管理入口与工作台/API 对齐；矩阵与 clean-room 简表已回写。

**可自动 L2 已完成。** 证据见 `docs/COMMERCIAL_RELEASE.md` L2 快照与 `artifacts/sbom/`。

**商用公网发布仍依赖：**
1. 真实厂商 smoke 密钥与归档
2. 真实 SMTP smoke
3. GitHub Actions 账单恢复后的 CI 归档
4. 正式 SPDX/法务与模型/肖像/声音授权归档

后续工作进入发布运维/账号验收，不再作为「功能复刻缺口」处理。

## 7. 验收清单

- [x] DramaView 角色新增/编辑/删除
- [x] DramaView 角色生成形象/试听/存入角色库
- [x] DramaView 场景新增/编辑/删除
- [x] DramaView 场景生成图/复制/迁移
- [x] DramaView 剧集编辑标题与 AI 服务绑定
- [x] E2E 覆盖项目资产页角色/场景/剧集编辑
- [x] 推到主干（功能与文档提交已上 `main`）
- [x] 回写 `FUNCTIONAL_PARITY.md` / `CLEAN_ROOM.md`
- [x] 可自动 L2：SBOM、依赖扫描、Compose 冷启动/备份
- [ ] 外部 L2：真实厂商/SMTP/正式 SPDX 法务归档（有意保留，需外部账号）
